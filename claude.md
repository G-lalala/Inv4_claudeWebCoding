# Claude 開発ガイドライン

このドキュメントは、Claude Code を使用した開発プロセスと、仕様・タスク管理の方法を定義します。

## ドキュメント構成

### ディレクトリ構造

```
docs/
├── feature-name-1/
│   ├── spec.md
│   └── task.md
├── feature-name-2/
│   ├── spec.md
│   └── task.md
└── ...
```

### ファイルの役割

#### `spec.md` (仕様書)
- **目的**: 機能や仕様の詳細を記録
- **内容**:
  - 機能の概要
  - 要件定義
  - ユーザーから伝えられた内容を詳細にドキュメント化
  - 技術的な仕様
  - 制約事項
  - 画面設計やデータモデル（必要に応じて）

#### `task.md` (タスク一覧)
- **目的**: 仕様を実現するための作業内容を明確化
- **内容**:
  - 実装に必要な作業の一覧
  - 各タスクの詳細説明
  - タスクの優先順位
  - 依存関係
  - チェックリスト形式での進捗管理

## 開発プロセス

### 1. 新機能開発の開始

1. ユーザーから機能の要望を受け取る
2. `docs/` ディレクトリ配下に機能名のディレクトリを作成
3. `spec.md` を作成し、ユーザーの要望を詳細にドキュメント化
4. `task.md` を作成し、実装に必要なタスクを洗い出し

### 2. タスクの実行

- ユーザーから「このタスクを実行してください」と依頼を受けたら実行開始
- `task.md` の内容に従って作業を進める
- 作業中は適宜 `task.md` を更新し、進捗を記録

### 3. PR（Pull Request）作成

PR を作成する際は、以下の情報を含める：

```markdown
## 概要
[機能の簡潔な説明]

## 仕様 (spec.md より)
[spec.md の内容を引用または要約]

## 実施したタスク (task.md より)
- [x] タスク1
- [x] タスク2
- [x] タスク3

## テスト
[実施したテストの内容]

## 備考
[その他の注意事項]
```

## 命名規則

### ディレクトリ名
- 機能を表す簡潔で分かりやすい名前
- ケバブケース（kebab-case）を使用
- 例: `user-authentication`, `database-setup`, `api-integration`

### ファイル名
- `spec.md`: 仕様書（固定）
- `task.md`: タスク一覧（固定）

## ドキュメント管理のルール

1. **詳細に記録する**: ユーザーから伝えられた内容は、できるだけ詳細に `spec.md` に記録
2. **タスクを明確にする**: `task.md` には、実装者が迷わないレベルまで作業を分解
3. **常に最新に保つ**: 仕様変更やタスクの追加があった場合は、随時更新
4. **参照を容易にする**: PR や commit メッセージから該当する spec/task を参照できるようにする

## 例

### 例: ユーザー認証機能の場合

```
docs/
└── user-authentication/
    ├── spec.md      # ユーザー認証の仕様詳細
    └── task.md      # ユーザー認証実装のタスク一覧
```

**spec.md の例**:
```markdown
# ユーザー認証機能 仕様書

## 概要
ユーザーがメールアドレスとパスワードでログインできる機能

## 要件
- メールアドレスとパスワードによる認証
- JWT トークンを使用したセッション管理
- パスワードは bcrypt でハッシュ化
...
```

**task.md の例**:
```markdown
# ユーザー認証機能 タスク一覧

## タスク
- [ ] データベースに users テーブルを作成
- [ ] ユーザー登録 API エンドポイントを実装
- [ ] ログイン API エンドポイントを実装
...
```

## 注意事項

- 複雑な機能は、さらにサブディレクトリに分割しても良い
- spec.md と task.md は常にセットで作成する
- 作業前に必ず spec.md を確認し、仕様を理解する
- タスク完了後は task.md のチェックボックスを更新する
